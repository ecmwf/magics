---
LineStyle:
  documentation: hello
  values:
    solid: la doc
    dash: la doc
    dot: la doc
    chain_dash: la doc
    chain_dot: la doc

ListPolicy:
  documentation: hello
  values:
    lastone: la doc
    cycle: la doc

Justification:
  documentation: hello
  values:
    left: la doc
    centre: la doc
    right: la doc

Position:
  documentation: hello
  values:
    automatic: la doc
    top: la doc
    bottom: la doc
    left: la doc
    right: la doc
